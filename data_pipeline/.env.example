# NeonDB PostgreSQL Database Connection
# Replace with your actual NeonDB connection string
DATABASE_URL='postgresql://username:password@host/database?sslmode=require'

# Optional: Google Cloud credentials for DVC remote storage
# Uncomment if using GCS for DVC
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: Airflow configuration
# AIRFLOW_HOME=/path/to/data_pipeline/airflow
